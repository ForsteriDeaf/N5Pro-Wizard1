# Correções aplicadas

1. `N5PRO_REPO_BASE` corrigido para apontar para `main/final` no repositório GitHub.
2. Fluxo do `n5pro-post` fica coerente com a estrutura real dos ficheiros remotos.
3. `01-PVE_Host-PostInstall.sh` voltou a incluir o ramo `else` para `/etc/apt/sources.list`.
4. `02-Unraid-NAS_PVE-CreateVM.sh` voltou a detetar preferencialmente o NVMe Lexar.
